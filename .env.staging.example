# Optional shared staging vars (if you prefer a single file).
# Keep secrets out of git. Copy to `.env.staging` (ignored) and fill in values.

# Example: staging app URLs (non-secret)
APP_TEST_URL=https://app-test.example.com
API_TEST_URL=https://api-test.example.com

